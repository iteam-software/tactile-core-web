
/**
 * System base class.
 */
export class System {
  /**
   * Create a new system.
   * @param {object} store The redux store.
   */
  constructor(store) {}
  /**
   * Update the system.
   * @param {number} delta The engine delta.
   */
  update(delta) {}
}
